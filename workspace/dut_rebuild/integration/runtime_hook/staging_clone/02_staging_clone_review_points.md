# Staging Clone Review Points

1. Verify compatibility package can be instantiated per rule
2. Verify first-warning aggregation logic is acceptable
3. Verify roundtrip does not lose critical action fields
4. Confirm that runtime action generation is intentionally NOT migrated in this clone
5. Decide where future V2 core will replace the placeholder section
