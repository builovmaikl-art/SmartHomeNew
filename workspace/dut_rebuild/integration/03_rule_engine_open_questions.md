# Rule Engine open questions

1. Should day-of-week remain numeric enum or become dedicated value type?
2. Should door/window be split into two sources instead of combined semantics?
3. Should action domain be separated from action type at runtime adapter level?
4. Do we need explicit BOOL action value precedence over REAL?
5. Should trigger policy remain outside core permanently?
